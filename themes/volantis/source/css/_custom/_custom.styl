/* =========================
   语言切换下拉菜单样式
   位置：搜索框左边
   功能：默认显示语言图标，鼠标悬停时展开 US / JA 菜单
========================= */
.nav-lang-switch
  position: relative
  display: inline-block
  margin-right: 10px

.nav-lang-switch .lang-toggle
  font-size: 1.3em
  color: inherit
  cursor: pointer

/* 下拉菜单容器 */
.nav-lang-switch .lang-menu
  display: none !important
  position: absolute
  top: 100%
  left: 0
  background: var(--color-card)
  border-radius: 6px
  box-shadow: 0 2px 6px rgba(0,0,0,0.15)
  padding: 5px 0
  min-width: 100px
  z-index: 2000

/* 下拉菜单项 */
.nav-lang-switch .lang-menu li
  list-style: none

/* 每个选项（国旗 + 文本） */
.nav-lang-switch .lang-menu a
  display: flex
  align-items: center
  gap: 6px
  padding: 6px 12px
  text-decoration: none
  color: var(--color-text)
  font-size: 0.9em
  white-space: nowrap

/* 悬停效果 */
.nav-lang-switch .lang-menu a:hover
  background: var(--color-block)
  color: var(--color-theme)

/* 悬停时显示菜单 */
.nav-lang-switch:hover .lang-menu
  display: block !important

/* ==== 语言切换下拉菜单样式结束 ==== */
body::before
  content: "✅ custom.styl loaded"
  position: fixed
  top: 10px
  left: 10px
  background: green
  color: white
  padding: 5px
  z-index: 9999
